Collections:
is a framework in java having many different interfaces and classes that help us dealing with
different types of data.
Collection is a group of objects represented as a single entity
it is dynamic, can grow and shrink the size in runtime

ArrayList is a class in the collection framework that can store the single objects
Properties:
1. Arrays can have duplicate objects
2. Arrays keep the insertion order
3. Best choice when you are accessing and reading (not modifying) the data

LinkedList:
1. Arrays can have duplicate objects
2. Arrays keep the insertion order
3. Best choice when you modifying the data

Set:
Set is a data structure that stores unique objects
Set is an unordered collection of objects in which duplicate values cannot be stored

Properties:
1. can not store duplicate elements


